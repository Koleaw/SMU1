---
import CatalogV2Layout from '../CatalogV2Layout.astro';
import '../../../styles/v2/direction-v2.css';
import '../../../styles/v2/immersive-direction-v2.css';

interface Props {
  title: string;
  description: string;
  canonicalPath: string;
  currentPath: string;
  ogImage?: string;
  variant?: 'engineering-visual' | 'visual' | 'engineering-commercial' | 'project-commercial' | 'place-commercial';
  productFinalHandoff?: boolean;
  contactHref?: string;
  ctaLabel?: string;
  headerCtaBinding?: Record<string, string>;
  headerMobileCtaBinding?: Record<string, string>;
  mobileCtaBinding?: Record<string, string>;
}

const {
  title,
  description,
  canonicalPath,
  currentPath,
  ogImage = '',
  variant = 'engineering-visual',
  productFinalHandoff = false,
  contactHref = '#contact',
  ctaLabel
} = Astro.props as Props;
---

<CatalogV2Layout
  title={title}
  description={description}
  canonicalPath={canonicalPath}
  currentPath={currentPath}
  ogImage={ogImage}
  pageKind="section"
  contactHref={contactHref}
  headerCtaLabel={ctaLabel}
  mobileCtaLabel={ctaLabel}
  headerCtaBinding={Astro.props.headerCtaBinding}
  headerMobileCtaBinding={Astro.props.headerMobileCtaBinding}
  mobileCtaBinding={Astro.props.mobileCtaBinding}
  headerMode="overlay"
  rootClass="catalog-v2--immersive"
  productFinalPrototype={productFinalHandoff ? 'direction' : undefined}
>
  <div class:list={['direction-v2', `direction-v2--${variant}`]} data-direction-v2-root>
    <slot />
  </div>
</CatalogV2Layout>
