# Локальная админка СМУ-1 (Stage 1)

## Основной сценарий (visual edit mode)
1. Откройте `http://127.0.0.1:4321/SMU1/admin/`.
2. Войдите (`admin / admin`).
3. Нажмите карточку **«Редактировать страницу Навесы»**.
4. Откроется visual edit mode: `http://127.0.0.1:4321/SMU1/admin/pages/navesy/`.

Это основной рабочий путь Stage 1.

## Технический режим
Старый технический редактор сохранен как запасной:
- `http://127.0.0.1:4321/SMU1/admin/technical/`

По умолчанию он не используется.

## Запуск
1. `npm run dev -- --host 127.0.0.1`
2. `npm run admin:api`
3. Открыть `http://127.0.0.1:4321/SMU1/admin/`

## Верхняя панель редактора
В visual edit mode сверху фиксированная панель:
- `← В админку`
- `Редактирование: Навесы`
- статус изменений
- `Сохранить`
- `Открыть публичную страницу`
- `Выйти`

## Inline-редактирование hero
В hero есть кнопка **«Редактировать hero»**.

После нажатия редактирование происходит на месте:
- заголовок;
- описание;
- подпись placeholder;
- `Заменить фото` (upload);
- `Убрать фото`;
- `Готово` / `Отмена`.

## Inline-редактирование карточек
В блоках `cardGrid/exampleGrid` карточки редактируются прямо внутри карточки:
- кнопки `Редактировать`, `Выше`, `Ниже`, `Скрыть/Показать`;
- в режиме редактирования: заголовок, описание, активность, `Готово`, `Отмена`.

## Добавление карточки
Внизу блока кнопка **`+ Добавить карточку`**.

Новая карточка создается со значениями:
- `title: "Новая карточка"`
- `text: ""`
- `order: следующий`
- `isActive: true`

## Списки и шаги
- `listPanel/factorList`: кнопка `Редактировать список`, inline-редактирование строк, `Выше/Ниже/Удалить`, `+ Добавить строку`.
- `process`: inline-редактирование шагов, `Выше/Ниже/Скрыть`, `+ Добавить шаг`.

## Настройки блока
В шапке каждого блока кнопка **«Настройки»**.
Открывается модальное окно с полями:
- Заголовок;
- Intro;
- Тема;
- Сетка (если применимо);
- Колонки (если применимо);
- Активность;
- `Готово` / `Отмена`.

## Загрузка фото
Добавлен endpoint:
- `POST /api/admin/upload`

Требования:
- только с авторизацией;
- `multipart/form-data`;
- типы: `jpg`, `jpeg`, `png`, `webp`, `svg`;
- максимум 10 MB;
- файлы сохраняются в `public/uploads`;
- ответ: `{ "path": "/uploads/file-name.ext" }`.

Где работает upload:
- hero image;
- `mediaText` блоки (поле `media`);
- `process` блоки (поле `media`).

## Сохранение
Кнопка **«Сохранить»** отправляет:
- `PUT /api/admin/content/product-sections/navesy`

После успеха:
- сообщение `Сохранено`;
- dirty state сбрасывается;
- без полной перезагрузки страницы.

## Debug JSON
JSON доступен только как отладочная секция внизу под `Показать JSON`.

## Ограничения Stage 1
Пока не реализовано:
- drag-and-drop;
- массовый visual editor для всех страниц;
- production hosting backend;
- GitHub write mode.
