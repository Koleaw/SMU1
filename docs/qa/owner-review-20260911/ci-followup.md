# Проверка регрессии добавления фотографий

Первая CI-проверка коммита `59f008f7cdef0bc11e62d916253a0f6bf82647c9` обнаружила реальную ошибку геометрии редактора. Это не объявлено случайным сбоем и не исправлялось повторным запуском без изменения кода.

В запуске `34539310858`, job `103078174591`, не прошли `bulk-media-queue`, `reload-recovery` и `bulk-media-stage-save`. Все 20 корректных файлов получили HTTP 201 при подготовке медиа. Два ответа HTTP 400 относятся к намеренно испорченному тестовому файлу и его повтору. После позднего исключения сценарий не вернул разрешение на эти ожидаемые ответы, поэтому отчёт дополнительно посчитал их ошибками HTTP.

Причину подтвердил отдельный Chromium-проход на изолированной копии текущего сайта. Публичное desktop-правило задавало `width:auto` контейнеру галереи. В редакторе этот же контейнер при отсутствии фотографии служит абсолютной областью «Добавить фотографии», внутри которой тоже находится абсолютная кнопка. Её ширина схлопнулась до нуля. Старый тест выбирал первый ненулевой media-binding и попадал в связанную карточку другого изделия, затем ожидал черновик исходного изделия.

| Фактическое измерение | Ширина × высота | Владелец binding |
| --- | --- | --- |
| Исходная область добавления фото | 0 × 412,58 px | `konteynernaya-ploshchadka-modul` |
| Следующая связанная карточка, выбранная старым тестом | 176 × 46 px | `konteynernaya-ploshchadka-zakrytaya` |
| Временное восстановление задуманной половинной ширины в диагностическом браузере | 687,5 × 412,58 px | `konteynernaya-ploshchadka-modul` |
| Промежуточный CSS до cc2701 при ширине canvas 1440 / 390 / 320 px | 220 × 50 px | `konteynernaya-ploshchadka-modul` |

Desktop-правило публичной галереи ограничено реальным дочерним компонентом: `:has(> .v2-product-gallery)`. После исключения редакторской области визуально обнаружен прежний каскад `width:100vw`. Поэтому собственные ширина, высота и нулевой внешний отступ закреплены через `!important` только в CSS редактора. Это не позволяет публичным правилам первого экрана переопределить размеры служебной кнопки.

На этапе 219a6bee область компактна: максимум 220 × 50 px, на desktop не шире половины родительского контейнера. Она располагается справа внизу. При ширине до 980 px только первый экран без фотографии в редакторе получает 64 px нижнего пространства под кнопку. Это устраняет пересечение со ссылкой Telegram, выявленное на промежуточном мобильном проходе. Публичная страница и редакторские страницы с фотографией этого отступа не получают.

Сам acceptance-сценарий усилен: `no-photo-affordance` сохраняет фактические размеры, требует область не меньше 44 × 44 px, не шире половины родителя и точного владельца исходного изделия. Все три поиска media-binding для очереди, восстановления и сохранения теперь явно задают `ownerSlug`. Архитектура редактора, хранилище черновиков и API загрузки не перестраивались.

Завершающий Chromium-проход по фактическому source выполнен на ширинах iframe 1440, 390 и 320 px. Во всех трёх случаях область составляет 220 × 50 px; проверка пересечений с текстовыми строками заголовка, описания, цены, преимуществ и действий вернула пустой список. Шесть снимков первого экрана и кнопки после прокрутки лично просмотрены: длинный заголовок переносится, подпись кнопки читается, текст и действия не перекрыты. Настоящий клик на 320 px открыл окно массовой загрузки именно для «Контейнерная площадка Модуль» с полем выбора файлов; Escape закрыл окно. Файлы не добавлялись и не сохранялись.

Локально выполнены 11 тестов `visual-editor-acceptance.test.mjs`: 11 PASS. Геометрические диагностические проходы не сохраняли контент и не вызывали release-операции. Полный повтор CI и финальная проверка опубликованной версии на момент этой записи ещё ожидаются.

Доказательства вне репозитория: `E:/CodexTemp/smu1-owner-20260911/media-geometry.json`, `media-geometry-before.png`, `media-geometry-after.png`, `media-geometry-source-after.json`, `media-geometry-source-after.png`. Последние два файла показывают промежуточное исключение из публичного правила, а не окончательный UI-проход.

Доказательства этого промежуточного этапа в той же папке: `media-affordance-final.json` (`passed: true`), `media-affordance-final-{1440,390,320}-{hero,control}.png` и `media-affordance-final-dialog.png`. Все процессы изолированного браузера, Astro и API закрыты после проверки.

Отдельный CI-сбой проверки `beforeunload` локально не воспроизвёлся: исходный файл прошёл 9/9. Fixture дополнен подтверждением установленного обработчика, настоящего клика и sticky activation перед прежней проверкой отказа навигации. Чтение состояния не создаёт user gesture. Защита CDP не ослаблена; целевой браузерный тест прошёл 1/1 без пропуска (`beforeunload-readiness-target.log`). Причина исходного CI-сбоя не объявляется доказанной.

После ограничения публичного селектора и уточнения светлого клавиатурного контура выполнена сборка 229 страниц, затем штатная очистка недостижимых медиа. H5: 22/22, 420540767 байт, 4373 файла, одно прежнее мягкое предупреждение LCP благоустройства; бюджеты не менялись (`h5-ci-fix.json`). Статическая проверка с конфигурацией test и `/SMU1/`: 38/38, 107 маршрутов, 9296 ссылок, 15213 ссылок на медиа (`static-ci-fix-correct-env.log`). В публичных CSS отсутствуют селекторы области редактирования. 13 тестов acceptance/геометрии полноэкранной галереи прошли; они не заменяют повтор полного браузерного CI.

Адресный проход фокуса выявил два соседних дефекта. Специфичность общего правила галереи переопределяла светлый контур в «Волне» и «Портале»; дополнительное условие публичного контейнера устранило конфликт без `!important`. Во всех трёх движках повторно подтверждён белый контур 3 px на полноэкранных кнопках этих двух изделий. У навесов собственный переход цвета SVG-path запускался позже перехода контейнера: промежуточно получалась белая стрелка на белом фоне. Для активных полноэкранных кнопок сохранены светлая стрелка и тёмная нейтральная поверхность `#304151`. Окончательная сборка после этого изменения снова прошла H5: 22/22, 420541012 байт, 4373 файла (`h5-gallery-contrast.json`).

Завершающий повтор навесов прошёл в Chromium, Firefox и WebKit: после отсутствия активных переходов во всём SVG-поддереве реальный stroke белый, фон `#304151`, контраст 10,50:1, контур 2 px, Escape возвращает фокус. Снимки просмотрены (`final-gallery-focus/report.json` и PNG). Равные половины «Волны» при 1366/390 px подтверждены во всех трёх движках; обе CTA на тематических панелях проверены настоящим Tab в Chromium/Firefox.

У установленного Windows WebKit обычный Tab и Alt+Tab пропускают ссылки. Это отдельно воспроизведено на простом HTML с двумя ссылками, input и button без кода/стилей СМУ-1. Поэтому три сценария достижения hero-ссылок через Tab не обозначены успешными. Их CSS-контур дополнительно проверен на «Волне» программной установкой фокуса после клавиатурного ввода: белый, 2 px, `:focus-visible`. Этот способ не выдан за достижение ссылки через Tab. Управление настоящими кнопками галереи, поиска и меню в WebKit проверено отдельно.

## CI коммита 219a6bee: действия редактора и адаптивная типографика

В запуске `34542240660` для SHA `219a6beeb5ec4a8cd80fb20d4cf6eecbe6ca0e83` успешно проверены **544 зарегистрированных действия редактора, 566 выполнений и 111 маршрутов canvas**. Это результат проверки действий; он не обозначает успешную полную acceptance-проверку, сборку или публикацию.

Build job `103087209825` остановился на четырёх устаревших ожиданиях размера H1 раздела ограждений и итоговой сводной проверке responsive. При 1920/1440/1280/1181 px фактические размеры — 72/61,2/54,4/50,1925 px — точно соответствуют принятому CSS `clamp(46px, 4.25vw, 72px)`. Проверка продолжала ожидать 96/77,76/72/72 px. Геометрия шапки, отсутствие переполнения документа и остальные условия этих четырёх строк прошли.

Тот же отчёт выявил пять реальных случаев переполнения текста внутри колонок: H1 изготовления на заказ на 20 и 65 px при 1280/1181 px, H1 объекта на 9 px при 1181 px и адрес карты на 3 и 5 px при 1181/320 px. Исходный H2 записывал их только в `longTextIssues`; нулевое переполнение всего документа позволяло основной проверке пройти. Эти случаи не объявлены безобидными предупреждениями: исправляются композиции трёх страниц и усилен обязательный контроль текста.

| Изменение контракта H2 | Точная область действия |
| --- | --- |
| Заголовок разделённого hub | Только `section-hub` при ширине от 981 px: `clamp(46px, 4.25vw, 72px)` |
| Заголовок изготовления на заказ | Только `custom-order` при 1101–1320 px: согласованный CSS `clamp(64px, 6.1vw, 80px)`; прежняя шкала вне диапазона сохранена |
| Переполнение текста | Уже измеренные H1, H2 и lead до и после прокрутки: более 2 px теперь проваливает `overflowOk` и общий responsive gate. H2 адреса карты включён существующим селектором `.practical-v2 .practical-contacts__details h2` |
| Длинные заголовки | Четыре или пять строк при нулевом переполнении остаются предупреждением; количество строк само по себе не проваливает проверку |

Допуск размера шрифта 0,25 px, шкалы H2/lead, остальные семейства и проверки шапки/документа не ослаблены. Лёгкий набор контрактов прошёл **9/9**: границы и линейные участки обеих формул, неизменность других семейств, реальные переполнения 20/65/9 px, адрес карты, lead и допустимые многострочные случаи. Повторная оценка сохранённого CI-отчёта подтвердила четыре точных совпадения шкалы hub, пять обязательных отказов по переполнению и шесть предупреждений без переполнения. Это обработка записанных измерений, а не новый браузерный проход и не успешный итоговый H2.

Доказательства: `E:/CodexTemp/smu1-owner-20260911/ci-job-103087209825.log`, `h2-responsive-contracts.log`, `h2-type-scale-recorded-recheck.json` и `h2-text-overflow-recorded-recheck.json`.

## Последующее исправление области без фотографии

В `cc2701057f28b439dd994b6a2b04400165bc0f1e` (`cc2701`) редакторский первый экран без фотографии переведён в нормальный поток. Это последующее исправление заменяет описанную выше промежуточную абсолютную компоновку области добавления фото. Локальная адресная проверка прошла на **22 ширинах**, выполнены **6 настоящих открытий** управления добавлением фотографии.

Эта локальная проверка подтверждает геометрию и открытие управления, но не заменяет полный сценарий загрузки, восстановления и сохранения. Успешный результат канонической полной acceptance-проверки ещё требуется. Итоговый CI, опубликованный SHA и preview остаются **pending**.


## Проверка первого клика после cc2701

Штатный isolated runner на чистой копии SHA `cc2701057f28b439dd994b6a2b04400165bc0f1e` всё ещё завершился с кодом 1. Первая попытка обнаружила потерю открытия галереи и последующие зависимые таймауты; отдельная отмена PNG зафиксирована во время перестановки карточек. Повтор с наблюдающим preload-скриптом выполнил очередь из 20 фотографий, восстановление после перезагрузки, подготовку и локальное сохранение, но итог не принят: два dev-script запроса были отменены при смене canvas document. Диагностический прогон не считается успешной канонической приёмкой.

В этой диагностической копии вручную подтверждена публикация всех 20 файлов только в физический изолированный `public/uploads` на E: пути соответствуют содержимому SHA-256, обложка входит в сохранённую галерею, неожиданных файлов и release-мутаций нет. Это проверка локального продвижения медиа в disposable fixture, а не обновление сайта владельца. Оригинальные content/data/uploads не менялись.

Причина потери первого щелчка затем воспроизведена адресно. Настоящие `pointerdown` и `mousedown` пришли на кнопку галереи изделия «Модуль». Между нажатием и отпусканием обычное сообщение геометрии вызвало `renderOverlay → replaceChildren`, удалив нажатую кнопку: `isConnected=false`. После native `mouseReleased` событие click не доставлено и окно осталось закрытым.

Узкое исправление удерживает DOM обычной нажатой overlay-кнопки до следующего кадра после отпускания/отмены; новые координаты по-прежнему поступают в состояние. Обновления объединяются и применяются после завершения click. Ручки native drag исключены из удержания: при автопрокрутке им нужна прежняя непрерывная геометрия. Потеря указателя, отмена жеста и уход фокуса освобождают удержание. Защита реального клика, проверка принадлежности binding и запрет release-операций сохранены.

Повторный native probe показал `isConnected=true`, все пять событий `pointerdown → mousedown → pointerup → mouseup → click` доставлены одной кнопке с `isTrusted=true`; открылась именно галерея «Контейнерная площадка Модуль». Снимок открытого окна лично просмотрен. Пять целевых unit-проверок прошли: сохранение цели до click, новая попытка до отложенного кадра, отмена указателя, потеря жеста/фокуса и отсутствие задержки для клавиатурного/обычного обновления.

Доказательства: `acceptance-cc2701.json`, `acceptance-cc2701-DIAGNOSTIC.json`, `diagnostic-media-promotion-proof.json`, `media-held-geometry-before.json/png` и `media-held-geometry-after.json/png` в `E:/CodexTemp/smu1-owner-20260911`. Полная каноническая acceptance-проверка после исправления всё ещё ожидается.

## Повтор публичной геометрии текста

После исправления трёх публичных композиций root выполнил и лично просмотрел все 13 адресных снимков переполнения текста. Эта публичная сборка прошла H5 **22/22**, размер **420541787 байт**, **4373 файла**, с одним прежним мягким предупреждением; бюджеты не менялись. Статический проход: **38/38**, **107 семантических маршрутов**, **9296 ссылок**, **15213 проверок медиа**. Эти результаты относятся к публичной сборке до включения нового editor-only guard, не обозначают успешный финальный CI или публикацию preview.

Окончательный узкий повтор после исключения drag-ручек: `media-held-geometry-final.json`, `passed: true`, exit 0. Подготовка использует существующий `clickWhenReady` со всеми проверками видимости, перекрытия, стабильности и идентичности DOM. В диагностическом dispatch между native нажатиями вставлено только штатное сообщение геометрии. Один предыдущий запуск остановился до нажатия из-за отсутствующей ещё overlay-кнопки; это исправлено ожиданием её готовности и не выдано за успешный результат.


## Каноническая полная приёмка 4cc5d7a

Неизменённый `tools/qa/isolated-admin-action-qa.mjs --stage=acceptance` выполнен на чистом локальном клоне точного SHA `4cc5d7a21068a32402f37ec495fadd8ca641389e`, без диагностического preload: **exit 0, 20/20 сценариев и 22/22 итоговых проверок PASS**. В том числе прошли перестановка карточек, область добавления фото, очередь из 20 изображений, восстановление после перезагрузки, обычное сохранение/exact, подготовка и сохранение галереи, независимые роли медиа объекта, конфликты вкладок и защита черновика при сбоях.

Штатный оркестратор дополнительно проверил сохранённую галерею «Модуля»: **20 физических файлов**, для каждого имя соответствует SHA-256 содержимого, явно выбранная обложка входит в галерею. Все записи находятся в `E:/CodexTemp/smu1-h6-admin-actions/run-Tho06H/checkout`; `public` и `isolated-content` — физические каталоги этого disposable fixture. **Попыток запуска release и публикаций: 0; runtime-ошибок: 0; неожиданных HTTP/network-ошибок: 0.** Новая классификация отмен при замене iframe в этом проходе не понадобилась: отменялись только прежние частичные видео-запросы Media/206 при уходе со страницы.

После завершения исходные `src/content`, `src/data` и `public/uploads` не имеют изменений. SHA-256 действующей вакансии остался `2f8319111e8b1814422c4eadf3d727082b7b67d747c89abd821bce0d61dff676`, равным исходному. Процессы браузера/API/Astro завершены, тестовые порты закрыты.

Результаты: `E:/CodexTemp/smu1-owner-20260911/acceptance-4cc5d7a.json`, `acceptance-4cc5d7a.log`, `acceptance-4cc5d7a.exit.txt`. Это успешная полная локальная приёмка именно **4cc5d7a**. Последующая отдельная правка CSS контраста шапки, её итоговый CI и публикация preview на момент записи ещё ожидаются; локальная приёмка не выдана за их завершение.

## Завершающий контраст и граница разделённого экрана

На фотографии изготовления на заказ фон под поиском меняется со стены на светлую крышу вместе с компоновкой. Окончательное решение использует белую поверхность существующих кнопок поиска и меню и тёмный знак `#15232c` только над фотографией, при ширине от 701 px. Шапка остаётся прозрачной, новая градиентная подложка не добавлена. Ниже этого порога сохраняется светлое управление над синей текстовой панелью. Focus имеет тёмный контур со светлой подложкой, hover — светлую нейтральную поверхность.

Проверены десять сочетаний движка и ширины в Chromium, Firefox и WebKit: исходный проход содержит девять PASS, отдельный настоящий клавиатурный повтор Firefox 768 px также PASS. Все 35 сохранённых кадров шапки просмотрены. Контраст текста и значков над фотографией — 16,05:1; после прокрутки проверено завершённое нейтральное состояние. Исходный `custom-header-surface-final/report.json` не переписан: смешанный сценарий pointer-open → Escape вернул фокус, но Firefox не показал `:focus-visible`. Отдельный `custom-header-firefox-menu-final/report.json` проверяет настоящий Tab → меню → Enter → Escape с видимым контуром. Ранний Escape до штатной передачи фокуса в меню не выдан за завершённое открытие; окончательная проверка ждёт фактического фокуса на кнопке закрытия.

Просмотр полного кадра на 701 px выявил соседний скачок размера H1: 52 px в узкой половине против 46,848 px на 768 px. Существующая шкала `clamp(46px, 6.1vw, 64px)` продлена только для заголовка до 701 px; сетка не изменена. Четыре итоговых полных кадра 701/760/768/700 px просмотрены: внутреннее переполнение 701 px уменьшилось с 39 до 1 px округления, при этом до фотографии остаётся 39 px; на 760 px — с 11 до нуля. 700 и 768 px сохранились без регрессий. Точное ожидание H2 обновлено; пять целевых unit прошли. Доказательство — `custom-701-glyph-final/report.json` и PNG.

Эти последние изменения затрагивают публичные CSS и ожидание типографики; локальная каноническая приёмка редактора выше относится к 4cc5d7a. Полный CI на окончательном SHA и проверка опубликованного preview должны быть подтверждены отдельно.